# Backend API URL (your migrated server)
# VITE_API_URL=http://localhost:3000
# WebSocket URL (Socket.IO uses http, not ws)
# VITE_WS_URL=http://127.0.0.1:3000

# Development mode
VITE_NODE_ENV=development

VITE_API_BASE_URL=http://localhost:3000
VITE_WS_URL=http://127.0.0.1:3000

# LangChain is enabled by default
# Users can toggle it in the UI
