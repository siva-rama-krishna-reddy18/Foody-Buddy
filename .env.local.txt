# env file for local devtesting.
# COPY THE CODE BELOW INTO A FILE NAMED '.env.local' OR '.env.development' ON THE LOCAL ENVIRONMENT.

# Backend API and WebSocket URLs
# echo VITE_API_BASE_URL=http://127.0.0.1:8080/api/v1
# echo VITE_WS_URL=ws://127.0.0.1:8080

# Local development URLs (Comment out when deploying)
VITE_API_BASE_URL=http://localhost:3000/api/v1
VITE_WS_URL=http://localhost:3001
